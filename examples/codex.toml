# Merge into your Codex configuration and replace both absolute paths.
[mcp_servers.x5]
command = "node"
args = ["C:/path/to/x5-mcp/dist/index.js"]
startup_timeout_sec = 20
tool_timeout_sec = 300

[mcp_servers.x5.env]
X5_ENV_FILE = "C:/path/to/x5-mcp/.env"
